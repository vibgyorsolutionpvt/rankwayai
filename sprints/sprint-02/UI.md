# Sprint 02 — UI

- `/register`, `/login`, `/forgot-password`, `/reset-password`
- Simple authenticated `/app` stub page showing user email + logout
- No full dashboard chrome yet (can be minimal shell)
