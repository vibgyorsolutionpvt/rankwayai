# Sprint 01 — Acceptance

Historical note: the app now lives at the **repo root** (Laravel + Inertia). Original checklist below was for the early dual-app scaffold.

Sprint 01 outcomes (current):

- [x] Laravel app runs (`php artisan serve`)
- [x] `GET /api/health` returns status ok JSON
- [x] Home page loads
- [x] `docker-compose.yml` defines mysql, redis, mailhog
- [x] `.env.example` exists
- [x] `.github/workflows/ci.yml` exists
