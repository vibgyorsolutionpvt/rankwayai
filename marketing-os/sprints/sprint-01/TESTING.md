# Sprint 01 — Testing

1. `composer install && php artisan migrate && php artisan serve`
2. `npm install && npm run dev` (or `composer run dev`)
3. Open http://localhost:8000 — home loads
4. Open http://localhost:8000/api/health — JSON ok
5. `php artisan test`
