# Sprint 02 — Database

- Use Laravel default `users` table; add columns only if needed (`email_verified_at` already standard)
- Password reset + personal access tokens tables as required by Sanctum
- No workspace tables yet
