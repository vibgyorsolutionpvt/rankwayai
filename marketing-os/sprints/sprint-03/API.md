# Sprint 03 — API

- `GET/POST /api/workspaces`
- `GET/PATCH /api/workspaces/{id}`
- `POST /api/workspaces/{id}/members`
- `PATCH/DELETE /api/workspaces/{id}/members/{userId}`
- Workspace context via chosen standard (header or nested routes) — document in README snippet

403 when user is not a member.
